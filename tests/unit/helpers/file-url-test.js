import { fileUrl } from '../../../helpers/file-url';
import { module, test } from 'qunit';

module('Unit | Helper | file url');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = fileUrl(42);
  assert.ok(result);
});
